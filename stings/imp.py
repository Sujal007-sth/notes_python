name = "sujal is from  nepal"
print(name.replace("  ", " "))#replaces back vlaue with front
print(name) # strings are immutable which means that you 
            # you cannot change them by running fn on them